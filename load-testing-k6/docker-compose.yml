version: '3.9'
services:
  k6suite:
    build: .
    environment:
      - API_BASE_URL=http://localhost:8080
      - WS_BASE_URL=ws://localhost:8080/ws
      - OUT_DIR=/app/results
    volumes:
      - ./results:/app/results
    command: ["node", "dist/index.js", "smoke"]

